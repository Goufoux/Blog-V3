<nav class="navbar navbar-expand-lg navbar-dark bg-primary col-12">
    <a class="navbar-brand" href="/admin/user/">Gestion des utilisateurs</a>
    <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#user-gestion" aria-controls="user-gestion"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="user-gestion">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
                <a href="{{ base_url }}add" class="nav-link">
                    Ajouter
                </a>
            </li>
            {% if current == 'view' %}
                <li class="nav-item">
                    <a href="{{ base_url }}update?id={{ user.id }}" class="nav-link">
                        Modifier
                    </a>
                </li>
            {% endif %}
            {% if current == 'update' %}
                <li class="nav-item">
                    <a href="{{ base_url }}view?id={{ user.id }}" class="nav-link">
                        Visualiser
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>