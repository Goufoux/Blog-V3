{% extends "frontend/base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row">
            <h1 class="col-12 mt-3">Mot de passe oublié ?</h1>
            <p class="col-12">
                Indiquer l'adresse email de votre compte et nous vous enverrons un lien pour réinitialiser votre mot de passe.
            </p>
            <form action="#" method="POST" class="col-12 p-5">
                <div class="input-group mb-3">
                    <div class="input-group-prepend">
                        <span class="input-group-text" id="label-email">
                            <i class="fas fa-at"></i>
                        </span>
                    </div>
                    <input placeholder="Email" type="email" aria-describedby="label-email" class="form-control" name="email" id="email" required value="{{ request.post('email')|default('') }}">
                    {% if form.hasError('email') %}
                    <div class="col-12">
                        <span class="badge badge-danger">{{ form.errors('email') }}</span>
                    </div>    
                    {% endif %}
                </div>
                <div class="form-group text-right">
                    <button type="submit" class="btn btn-success">Valider</button>
                </div>
            </form>
        </div>
    </div>
{% endblock %}